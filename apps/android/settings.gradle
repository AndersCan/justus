pluginManagement {
  repositories {
    google()
    mavenCentral()
    gradlePluginPortal()
  }
}

dependencyResolutionManagement {
  repositoriesMode = RepositoriesMode.FAIL_ON_PROJECT_REPOS
  repositories {
    google()
    mavenCentral() // io.github.anderscan.ekrooh:bare-host publishes here (no credentials)
  }
}

rootProject.name = "justus-android"
include(":app")
